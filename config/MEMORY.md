# MEMORY.md — the agent's own notes (both engines read; hermes writes)
# Lives at $MERCURY_HOME/MEMORY.md after first boot. This empty default
# seeds the file; the agent fills it through the memory tool. Hand-edit
# freely — your edits are permanent (updates never overwrite).
