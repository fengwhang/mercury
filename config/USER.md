# USER.md — who the user is (both engines read; hermes writes)
# Lives at $MERCURY_HOME/USER.md after first boot. This empty default
# seeds the file; the agent fills it as it learns about you. Hand-edit
# freely — your edits are permanent (updates never overwrite).
